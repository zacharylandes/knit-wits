<nav>
  <%= link_to image_tag(asset_path("knitwits.png")), root_path  %>

    <ul class="menu menu-hover-lines">
      <li><%= link_to 'Home', root_path  %></li>
      <li><%= link_to 'Knits', items_path  %></li>
      <li><%= link_to 'Categories', categories_path  %></li>
      <li><%= link_to cart_path do %>


        <i class="fa fa-shopping-cart" aria-hidden="true"></i>
        <%= @cart.total_count %>
        <% end %></li>
        <% if current_admin? && logged_in? %>
        <li><%= link_to "Dashboard", admin_dashboard_path %></li>
        <li><%= link_to "Logout", logout_path, method: :delete %></li>
        <% elsif logged_in? %>
        <li><%= link_to "Account", dashboard_path %></li>
        <li><%= link_to "Logout", logout_path, method: :delete %></li>
        <% else %>
        <li><%= link_to "Login", login_path %></li>
        <% end %>
      </ul>
</nav>
<% if current_user %>
<div class="nav-logged-in">
  <p>Logged in as <%= current_user.username %></p>
</div>
<% end %>
