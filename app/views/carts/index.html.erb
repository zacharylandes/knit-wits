  <table class="cart-contents">
    <th>Item</th>
    <th>Price</th>
    <th>Quantity</th>
    <th>Subtotal</th>
  <% @items.each do |item| %>
    <td><%= item.title %></td>
    <td><%= item.price %></td>
    <td><%= @cart.count_of(item.id) %></td>
    <td><%= @cart.item_subtotal(item.id) %></td>
    <td><%= button_to "Remove from Cart", cart_path(item_id: item.id), method: :delete %></td>
  <% end %>
  </table>
